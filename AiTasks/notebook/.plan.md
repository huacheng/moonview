# Implementation Plan: Claude Code Notebook

## 概述

构建一个类似 JupyterLab 的 Notebook 系统，以 Claude Agent SDK 为"内核"，将交互过程固化为可共享、可重放的 HTML 文件。

> 💬 **关于 "Claude Agent SDK"**: 使用 SDK 是否还可以以订阅模式使用账户？
>
> **调查结论**: Agent SDK 目前**仅支持 API key 认证**，不支持 Pro/Max 订阅计费。Anthropic 明确禁止第三方开发者在 SDK 构建的产品中使用 claude.ai 登录或订阅额度。社区有非官方的变通方案（如 `claude_max`、`ai-sdk-provider-claude-code`），但不是官方支持的方式。因此本项目需要用户提供 `ANTHROPIC_API_KEY`。
>
> 参考: [Issue #559](https://github.com/anthropics/claude-agent-sdk-python/issues/559), [Issue #11](https://github.com/anthropics/claude-agent-sdk-typescript/issues/11)

> 💬 **替代方案：直接使用 Claude Code CLI**: 既然 Agent SDK 仅支持 API key，那直接使用 claude code 是否可以？在 tmux 之中启动 claude code 进程，将前端用户输入转发到 claude 输入命令行，将 claude 输出以 SSE/WebSocket 形式输出到前端 notebook，包括思考过程。
>
> **可行性分析**:
>
> **优势**:
> - 可使用 Pro/Max 订阅，无需 API key 和按量计费
> - `claude -p` headless 模式 + `--output-format stream-json` 提供结构化流式输出
> - `--verbose` + `--include-partial-messages` 可输出 token 级别事件（含思考过程）
> - `--continue` / `--resume <session_id>` 支持多轮对话
> - Claude Code 自带完整工具链（Read、Edit、Bash、Glob、Grep 等）
>
> **实现方式**:
> - 服务端通过 `child_process.spawn` 启动 `claude -p --output-format stream-json --verbose` 子进程
> - stdin 写入用户 prompt，stdout 逐行解析 JSON 事件流
> - 将解析后的事件通过 WebSocket/SSE 转发给前端
> - 无需 tmux —— Node.js `child_process` 已足够管理子进程生命周期
>
> **风险**:
> - 已知问题：stdin 输入超过约 7000 字符时可能产生空输出（需分片或写入临时文件）
> - Claude Code CLI 是面向开发者的工具，不是正式的 API 集成方式
> - 进程管理复杂度高于 SDK 的函数调用
> - 版本更新可能改变 CLI 输出格式
>
> **结论**: 技术上可行，且能显著降低使用成本（订阅制 vs 按量计费）。建议将架构调整为 **Claude Code CLI 子进程方案**，放弃 Agent SDK。核心变更：`agent.ts` 中用 `child_process.spawn('claude', ['-p', '--output-format', 'stream-json', '--verbose'])` 替代 `query()`。

> 💬 **关于 Node.js `child_process` 多轮对话**: 子进程方案是不是执行完一个 prompt 就退出了？下次输入需要再次启动新进程，重新加载上下文？
>
> **调查结论**: 是的，`claude -p` 是单次执行模式 —— 处理完一个 prompt 后进程即退出。多轮对话的实现方式如下：
>
> **会话恢复机制**:
> - 每次 `claude -p` 执行后，完整对话记录保存到 `~/.claude/projects/[编码路径]/[SESSION_UUID].jsonl`
> - 下次执行时使用 `--continue`（恢复最近会话）或 `--resume <session_id>`（恢复指定会话）
> - 恢复时，CLI 读取整个 JSONL 文件，将完整历史作为上下文发送给 API
> - 可通过 `--output-format json` 的返回值中提取 `session_id`，用于后续精确恢复
>
> **性能开销**:
> - 每次进程启动约 **~12 秒**冷启动开销（Node.js 初始化 + 系统 prompt 加载 + MCP 服务器启动 + JSONL 重放）
> - 全量历史 token 重发：第 N 轮会话需重发前 N-1 轮的所有内容（prompt caching 可缓解，5-60 分钟 TTL）
> - Agent SDK 的 warm session 仅需 ~2-3 秒/轮
>
> **对 Notebook 架构的影响**:
> - 每个 Cell 执行 = 一次 `claude -p` 进程启动 + 退出（~12 秒额外延迟）
> - Cell 间上下文连续性依赖 `--resume <session_id>` 机制
> - 长 notebook（多 cell）的上下文重传成本递增
> - 替代方案：`tmux send-keys` 可向交互模式的 Claude Code 发送输入（社区验证可行），但稳定性不如 `-p` 模式
>
> **建议**: Notebook 中每个 cell 应记录 `session_id`，后续 cell 执行时使用 `--resume <session_id>` 保持对话上下文。接受 ~12 秒/cell 的启动开销作为 trade-off（换取订阅制零 API 成本）。

> 💬 **tmux + 交互模式方案**: 直接用 tmux + claude 交互模式，进程常驻，不存在重新加载问题，前次输入处理完后直接等待下次输入。
>
> **调查结论**: 社区已有多个成熟项目验证此方案可行，且是目前最主流的集成方式。
>
> **三种方案对比**:
>
> | 方案 | 启动开销 | 输出格式 | 上下文连续 | 实现复杂度 |
> |------|---------|---------|-----------|-----------|
> | A: `claude -p` 每 cell 一个进程 | ~2-20 秒/cell | 干净 JSON | 需 `--resume` | 低 |
> | B: tmux + 交互模式（常驻进程） | 首次启动后为零 | 需从 JSONL 解析 | 自动保持 | 中高 |
> | C: `claude -p --input-format stream-json`（常驻管道） | 首次启动后为零 | 干净 JSON | 单进程内保持 | 中 |
>
> **方案 B (tmux) 详细分析**:
>
> **输入**: `tmux send-keys -t <session> -l "prompt" && tmux send-keys -t <session> "" Enter` — 在 PTY 层注入按键，成功触发 Ink 的 `onSubmit`（社区确认有效）
>
> **输出**: **不要**解析 `capture-pane` 的终端输出（Ink TUI 大量 ANSI 重绘，极难解析）。正确做法：
> - 监控 JSONL 会话文件 `~/.claude/projects/<hash>/<session-id>.jsonl`，按字节偏移轮询新消息（每 1-2 秒）
> - 注册 `Stop` hook 写入标记文件，作为完成信号
> - `capture-pane` 仅用于检测交互式对话框（权限请求、`AskUserQuestion`）
>
> **社区验证项目**:
> - **ccbot** (six-ddc/ccbot): Telegram-tmux 桥接，1 个 Telegram 话题 = 1 个 tmux 窗口 = 1 个 Claude 会话
> - **claudecode-telegram** (hanxiao): Python 桥接，使用 `Stop` hook 信号完成
> - **claude_code_agent_farm** (Dicklesworthstone): 20+ 个 Claude 代理并行运行在 tmux 面板中
>
> **方案 B 的风险**:
> - 已知 Issue #24108: 空闲状态下 `send-keys` 可能出现文字显示但不提交的问题
> - 需要处理 Claude 的交互式权限提示（通过 `--dangerously-skip-permissions` 或自动回复）
> - 完成检测依赖轮询 + hook，有 1-2 秒延迟
> - 无官方 API 保证，Ink 渲染逻辑可能随版本变化
>
> **推荐方案**: **方案 B (tmux + 交互模式)**，理由：
> 1. 零启动开销 — 对 Notebook 多 cell 场景至关重要
> 2. 上下文自动连续 — 无需 `--resume` 和 token 重传
> 3. 社区验证充分 — ccbot、claudecode-telegram 等生产级项目
> 4. JSONL 文件监控提供结构化输出 — 不依赖终端解析
> 5. 可使用 Pro/Max 订阅
>
> **核心架构变更**: 服务端启动 tmux 会话运行 `claude --dangerously-skip-permissions`，通过 `send-keys` 注入 prompt，通过 JSONL 文件监控 + `Stop` hook 获取输出。

> 💬 **参考项目: ai-cli-online** ([huacheng/ai-cli-online](https://github.com/huacheng/ai-cli-online)): 一个已上线的浏览器端 AI CLI 开发环境，与本项目目标高度重合。
>
> **ai-cli-online 核心架构**:
> - **后端**: Node.js + Express + **tmux** 会话持久化 + **node-pty** 管理伪终端
> - **前端**: React + Zustand + **xterm.js** (WebGL 加速渲染) + 递归分屏布局
> - **通信**: WebSocket **二进制协议**（1 字节前缀帧用于终端 I/O，JSON 帧用于控制消息），TCP Nagle 禁用 + 压缩
> - **会话管理**: 固定 socket 路径实现断线重连，tmux detach/attach 机制使进程在浏览器断开后继续运行
> - **安全**: Token 认证 + 路径遍历防护 + CSP + 速率限制
> - **部署**: `npx ai-cli-online` 一键启动，systemd + nginx 生产部署脚本
> - **规模**: 268 commits, v3.0.20, TypeScript 91.7%, MIT 协议
>
> **与本项目的关键差异**:
> - ai-cli-online 是 **Web 终端** — 直接将终端输出（含 ANSI）透传到 xterm.js 渲染
> - 本项目是 **Notebook** — 需要将 Claude 的响应解析为结构化 cell（文本/工具调用/工具结果），以 Markdown/代码块形式渲染
> - ai-cli-online 没有 cell 概念、没有 HTML 导出、没有重放功能
>
> **可直接复用的架构组件**:
> 1. **tmux + node-pty 后端**: 会话管理、进程持久化、断线重连 — 直接适用
> 2. **WebSocket 二进制协议**: 低延迟终端 I/O 传输方案 — 可参考但 notebook 主要传 JSON
> 3. **部署方案**: systemd + nginx 反向代理 — 可直接复用
> 4. **安全层**: Token 认证 + 路径防护 — 可直接复用
>
> **不适用的部分**:
> - xterm.js 终端渲染 — notebook 需要自定义 cell 渲染组件
> - 终端 I/O 直接透传 — notebook 需要 JSONL 解析层将输出转为结构化数据
>
> **结论**: ai-cli-online 验证了 tmux + node-pty + WebSocket 方案在生产环境中的可行性，其后端架构可作为本项目的直接参考。核心区别在于前端渲染层：终端透传 vs 结构化 Notebook UI。

### 扩展需求

1. **数据可视化**: 在 Notebook 中引入 d3.js 或 TradingView 支持数据可视化，Cell 输出可包含交互式图表
2. **Git 集成与变更追踪**: 将实现过程中每个位置的中间 md 文件都插入对应位置，点击后可展开显示具体文件内容变动。实际结合 git 来展示，后续将与 Claude Code 的每次输入/输出交互都提交到 git
3. **Slide 总结功能**:
   - Notebook 一键总结为 Slide，用于汇报演示
   - 多个工作任务可二次总结形成新的 Slide
   - Slide 与 Notebook 都支持再编辑与批注编辑
   - 后续引入语音批注功能 — 会议记录中对某页面的讨论直接形成下一步工作任务
4. **单文件打包**:
   - Notebook 与 Slide 作为一个 HTML 文件的两个 Tab 切换
   - 相关中间过程的 md 文件与数据也放在 index.html 任务目录下
   - 一键打包，所有信息形成一个文件，发送给他人可复现

## 架构设计

```
┌─────────────────────────────────────────────┐
│                  Browser                     │
│  ┌─────────────────────────────────────────┐ │
│  │          Notebook Frontend (React)       │ │
│  │  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐   │ │
│  │  │Cell 1│ │Cell 2│ │Cell 3│ │ ...  │   │ │
│  │  │prompt│ │prompt│ │prompt│ │      │   │ │
│  │  │output│ │output│ │output│ │      │   │ │
│  │  └──────┘ └──────┘ └──────┘ └──────┘   │ │
│  └──────────────┬──────────────────────────┘ │
│                 │ WebSocket                   │
└─────────────────┼───────────────────────────┘
                  │
┌─────────────────┼───────────────────────────┐
│  Server (Node.js)                            │
│  ┌──────────────┴──────────────────────────┐ │
│  │         Session Manager                  │ │
│  │  ┌──────────┐  ┌──────────┐             │ │
│  │  │ Agent SDK │  │ Notebook │             │ │
│  │  │  query()  │  │  Store   │             │ │
│  │  └──────────┘  └──────────┘             │ │
│  └─────────────────────────────────────────┘ │
└──────────────────────────────────────────────┘
                  │
                  ▼
        ┌─────────────────┐
        │  .notebook.json  │  ← Notebook 文件
        │  (持久化格式)     │
        └────────┬────────┘
                 │ export
                 ▼
        ┌─────────────────┐
        │ notebook.html    │  ← 自包含 HTML
        │ (可共享/可重放)   │
        └─────────────────┘
```

## Notebook 文件格式 (.notebook.json)

借鉴 Jupyter .ipynb 但适配 Claude Agent SDK：

```json
{
  "version": 1,
  "metadata": {
    "title": "My Notebook",
    "created": "2026-02-18T00:00:00Z",
    "model": "claude-sonnet-4-6",
    "system_prompt": "",
    "allowed_tools": ["Read", "Edit", "Bash", "Glob", "Grep"],
    "cwd": "/path/to/project"
  },
  "cells": [
    {
      "id": "cell-uuid-1",
      "type": "prompt",
      "source": "Review utils.py for bugs and fix them",
      "outputs": [
        {
          "type": "assistant_message",
          "content": [
            { "type": "text", "text": "I'll review utils.py..." },
            { "type": "tool_use", "name": "Read", "input": {"file_path": "utils.py"} }
          ],
          "timestamp": "2026-02-18T00:01:00Z"
        },
        {
          "type": "tool_result",
          "tool_use_id": "toolu_xxx",
          "content": "file contents..."
        },
        {
          "type": "assistant_message",
          "content": [
            { "type": "text", "text": "Found a bug..." },
            { "type": "tool_use", "name": "Edit", "input": {...} }
          ]
        }
      ],
      "execution_count": 1,
      "status": "completed",
      "cost_usd": 0.05,
      "duration_ms": 12000
    },
    {
      "id": "cell-uuid-2",
      "type": "markdown",
      "source": "## Analysis Results\n\nThe bug was in..."
    }
  ]
}
```

## Step 1: 项目脚手架

**目标**: 初始化 monorepo 项目结构

```
notebook-ai/
├── packages/
│   ├── server/          # Node.js 后端
│   │   ├── src/
│   │   │   ├── index.ts
│   │   │   ├── session.ts
│   │   │   ├── agent.ts
│   │   │   └── export.ts
│   │   ├── package.json
│   │   └── tsconfig.json
│   └── web/             # React 前端
│       ├── src/
│       │   ├── App.tsx
│       │   ├── components/
│       │   │   ├── Notebook.tsx
│       │   │   ├── Cell.tsx
│       │   │   ├── CellInput.tsx
│       │   │   ├── CellOutput.tsx
│       │   │   └── Toolbar.tsx
│       │   ├── hooks/
│       │   │   └── useWebSocket.ts
│       │   └── types.ts
│       ├── package.json
│       └── tsconfig.json
├── packages/shared/      # 共享类型定义
│   ├── src/
│   │   └── types.ts
│   └── package.json
├── package.json          # workspace root
├── tsconfig.base.json
└── pnpm-workspace.yaml
```

**交付物**: 可运行的空项目，前后端 dev server 可启动

## Step 2: 共享数据模型 (packages/shared)

**目标**: 定义 Notebook 文件格式和 WebSocket 消息协议的 TypeScript 类型

核心类型:
- `Notebook` — 顶层文档结构
- `Cell` — prompt cell 或 markdown cell
- `CellOutput` — assistant_message / tool_use / tool_result / error
- `WSMessage` — WebSocket 消息协议 (execute_request, cell_output, execution_complete 等)
- `ExportOptions` — HTML 导出配置

## Step 3: 后端 Agent 服务 (packages/server)

**目标**: 用 Claude Agent SDK 实现 notebook "内核"

核心功能:
1. **SessionManager**: 管理多个 notebook 会话，每个会话维护一个 Agent SDK query 实例
2. **WebSocket 服务**: 接收 `execute_request`，调用 `query()`，流式转发 `SDKMessage` 给前端
3. **Notebook 持久化**: 读写 `.notebook.json` 文件
4. **HTML 导出**: 将 notebook 渲染为自包含 HTML

关键实现:
```typescript
// agent.ts — 核心执行逻辑
import { query } from "@anthropic-ai/claude-agent-sdk";

async function* executeCell(prompt: string, options: NotebookMetadata) {
  for await (const message of query({
    prompt,
    options: {
      allowedTools: options.allowed_tools,
      permissionMode: "bypassPermissions",
      cwd: options.cwd,
      model: options.model,
      includePartialMessages: true,
      systemPrompt: options.system_prompt || undefined,
    }
  })) {
    yield message; // 流式转发给前端
  }
}
```

## Step 4: 前端 Notebook UI (packages/web)

**目标**: React 实现 notebook 界面

核心组件:
1. **Notebook**: 顶层容器，管理 cells 数组
2. **Cell**: 单个 cell，包含输入区和输出区
3. **CellInput**: prompt 编辑器（文本区域 + 运行按钮）或 markdown 编辑器
4. **CellOutput**: 渲染 Claude 的响应（文本、工具调用、工具结果）
5. **Toolbar**: 文件操作（新建、打开、保存、导出 HTML）

关键交互:
- 用户在 CellInput 中输入 prompt，按 Shift+Enter 执行
- WebSocket 实时接收 Agent SDK 的流式消息
- 输出逐步渲染：思考文本、工具调用高亮、工具结果折叠
- 支持添加/删除/移动 cell

## Step 5: HTML 导出引擎

**目标**: 将 notebook 导出为自包含 HTML 文件

HTML 文件结构:
```html
<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>Notebook Title</title>
  <style>/* 内联所有 CSS */</style>
</head>
<body>
  <div id="notebook-root">
    <!-- 静态渲染的 cells -->
    <div class="cell" data-cell-id="xxx">
      <div class="cell-input">...</div>
      <div class="cell-output">...</div>
    </div>
  </div>

  <script id="notebook-data" type="application/json">
    // 完整的 notebook JSON 嵌入
  </script>

  <script>
    // 重放引擎：连接到服务器重新执行
    // 读取 notebook-data，逐 cell 发送 execute_request
  </script>
</body>
</html>
```

**两种模式**:
1. **静态查看**: 直接用浏览器打开，看到固化的输入/输出
2. **重放执行**: 连接到运行 notebook-ai server 的机器，重新执行所有 cell

## Step 6: 重放引擎

**目标**: 支持在另一台机器上重新执行 notebook

实现方式:
1. HTML 文件中嵌入完整的 notebook JSON (prompts + metadata)
2. HTML 文件中包含一个轻量的重放 UI + WebSocket 客户端
3. 用户在另一台机器上:
   - 启动 `notebook-ai server`
   - 用浏览器打开共享的 HTML 文件
   - 点击"重放"按钮，连接到本地 server
   - 逐 cell 重新执行，用新输出替换旧输出

```typescript
// 重放流程
async function replay(notebook: Notebook, serverUrl: string) {
  const ws = new WebSocket(serverUrl);
  for (const cell of notebook.cells) {
    if (cell.type === "prompt") {
      ws.send(JSON.stringify({
        type: "execute_request",
        cell_id: cell.id,
        source: cell.source
      }));
      // 等待 execution_complete
    }
  }
}
```

## Step 7: 集成测试 & 文档

**目标**: 确保端到端工作流正确

测试场景:
1. 创建 notebook -> 添加 cell -> 执行 -> 查看输出
2. 保存 notebook -> 重新打开 -> 内容完整
3. 导出 HTML -> 浏览器打开 -> 静态查看正确
4. 导出 HTML -> 另一台机器重放 -> 新输出正确生成

## 技术决策

| 决策 | 选择 | 理由 |
|------|------|------|
| 包管理器 | pnpm | 高效的 monorepo 支持 |
| 前端框架 | React + TypeScript | 生态成熟、组件模型适合 cell UI |
| 后端框架 | Express + ws | 简单可靠、WebSocket 原生支持 |
| 构建工具 | Vite (前端) + tsx (后端) | 快速开发体验 |
| Notebook 格式 | JSON (.notebook.json) | 类似 .ipynb，易于序列化、diffable |
| HTML 导出 | 自定义模板引擎 | 需要嵌入重放逻辑，nbconvert 不适用 |

## 风险与缓解

| 风险 | 缓解措施 |
|------|----------|
| API key 安全 | key 仅在 server 端使用，HTML 导出中不包含 |
| 大输出性能 | 流式渲染 + 虚拟滚动 + 输出截断 |
| WebSocket 断连 | 自动重连 + 请求队列 |
| 重放一致性 | 重放使用相同 model/prompt/tools，但输出不保证完全一致（LLM 特性）|
| 文件系统访问 | server 端的 cwd 配置，重放时需要相同的项目目录结构 |
